{% extends 'base.html.twig' %}

{% block title %}Proverb{% endblock %}

{% block body %}
    <h1>Proverb</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ proverb.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ proverb.title }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ proverb.auteur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('proverb_index') }}">back to list</a>

    <a href="{{ path('proverb_edit', {'id': proverb.id}) }}">edit</a>

    {{ include('proverb/_delete_form.html.twig') }}
{% endblock %}